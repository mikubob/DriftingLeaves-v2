<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.mapper.ViewMapper">

    <!-- 获取指定时间范围内的访问统计 -->
    <select id="getDailyViewStats" resultType="com.xuan.dto.DailyViewCountDTO">
        select date(view_time) as date, count(*) as count
        from views
        where view_time >= #{begin}
          and view_time &lt; date_add(#{end}, interval 1 day)
        group by date(view_time)
        order by date
    </select>

</mapper>
