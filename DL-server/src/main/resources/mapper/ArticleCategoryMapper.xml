<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xuan.mapper.ArticleCategoryMapper">

    <!-- 获取所有分类及其已发布文章数（包括没有文章的分类） -->
    <select id="getVisibleCategories" resultType="com.xuan.entity.ArticleCategories">
        select ac.*, count(a.id) as article_count from article_categories ac
        left join articles a on ac.id = a.category_id and a.status = 2
        group by ac.id
        order by ac.sort asc, ac.id desc
    </select>
</mapper>
